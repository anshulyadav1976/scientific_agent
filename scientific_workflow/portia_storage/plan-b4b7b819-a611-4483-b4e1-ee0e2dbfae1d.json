{
    "id": "plan-b4b7b819-a611-4483-b4e1-ee0e2dbfae1d",
    "plan_context": {
        "query": "\n        Analyze the dataset located at 'C:\\Users\\anshu\\scientific_agent\\scientific_workflow\\uploaded_files\\yoga_raw.txt'. The user provided a hint that the data type is 'unstructured'.\n        User context/request: 'take a look and lmk'\n\n        Follow these steps:\n        1. Ingest the data from the file path using the data_ingestion_tool, passing the provided data type hint ('unstructured' if data_type_hint else 'None') if available. The tool will output a dictionary containing a summary and the detected_type ('tabular' or 'unstructured'). Name the output of this step $ingestion_output.\n        2. **Conditionally Summarize**: If the 'detected_type' in $ingestion_output is 'unstructured', use the llm_tool to generate a concise summary of the 'snippet' found in $ingestion_output.summary. Ask the llm_tool: \"Provide a brief summary (2-3 sentences) of the following text snippet: [snippet content]\". Name the output of this step $llm_summary. If the detected_type is 'tabular', skip this step.\n        # Add step 3 for analysis in later phases (e.g., \"If detected_type is 'tabular', use AnalysisTool...\")\n        ",
        "tool_ids": [
            "data_ingestion_tool",
            "llm_tool",
            "search_tool"
        ]
    },
    "steps": [
        {
            "task": "Ingest the dataset from the file path 'C:\\Users\\anshu\\scientific_agent\\scientific_workflow\\uploaded_files\\yoga_raw.txt' using the provided data type hint 'unstructured'. The tool will return a summary and the detected type of the dataset. Name the output of this step as $ingestion_output.",
            "inputs": [],
            "tool_id": "data_ingestion_tool",
            "output": "$ingestion_output",
            "condition": null
        },
        {
            "task": "Generate a concise summary using the provided 'snippet' from the output summary of the ingestion step. Ask: 'Provide a brief summary (2-3 sentences) of the following text snippet: [snippet content]'. This should only be executed if the detected_type in $ingestion_output is 'unstructured'. Name the output of this step as $llm_summary.",
            "inputs": [
                {
                    "name": "$ingestion_output",
                    "description": "The output from the ingestion step containing the summary and detected_type."
                }
            ],
            "tool_id": "llm_tool",
            "output": "$llm_summary",
            "condition": "if $ingestion_output.detected_type is 'unstructured'"
        },
        {
            "task": "Reserve a step for future analysis of the dataset. This step will be used for additional in-depth analysis based on the ingestion output, such as processing tabular data if detected. Name the output of this placeholder step as $analysis_ready.",
            "inputs": [
                {
                    "name": "$ingestion_output",
                    "description": "The output from the ingestion step to be used for future analysis."
                }
            ],
            "tool_id": "llm_tool",
            "output": "$analysis_ready",
            "condition": null
        }
    ]
}